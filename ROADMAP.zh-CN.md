# Buzz Web 路线图

[English](ROADMAP.md) | [简体中文](ROADMAP.zh-CN.md)

本文只描述 Buzz Web 即将实施的工作，不承诺具体发布日期。Buzz 协议变化、真实用户反馈和
安全风险都可能调整优先级；任何功能只有在真实 Relay 上通过验收才算完成，Demo 数据能展示
不代表已经交付。

## 当前基线

Web 端已经具备主要协作闭环：身份登录、频道、私聊、线程、Reaction、@ 提及、搜索、
媒体、在线状态、邀请、Inbox、远程 Agent 发现与频道分配，以及只读 NIP-34 项目浏览。

后续版本重点补齐仍依赖 Buzz Desktop 的完整工作流，并让这个独立开源 Web 客户端更容易
安全部署和维护。

## P0：基础协作能力

> **状态：** 已实现并部署到测试环境；正式发布前仍需使用真实 Relay 成员身份完成验收。

### 频道和成员管理

**目标：** 增加频道发现、收藏、静音、归档、完整成员目录、角色变更和移除成员。

**验收标准：**

- owner/admin 按照与 Desktop 一致的 Relay 权限规则管理成员。
- 删除、移除、归档等操作必须二次确认，并安全展示 Relay 拒绝原因。
- 协议支持共享状态时，收藏、静音和归档在刷新及不同客户端之间保持一致。
- 非成员身份永远看不到私有频道。

### 消息生命周期、草稿和已读状态

**目标：** 实现消息编辑/删除、按会话保存草稿、明确的未读分界，以及协议支持时的
Relay 已读状态同步。

**验收标准：**

- 编辑和删除不会破坏线程、Reaction 及引用关系。
- 草稿按 Relay、身份和频道隔离，且绝不进入公开 `config.json`。
- Inbox 与频道未读标记在刷新及多客户端之间最终一致。
- Relay 拒绝操作后，界面不会遗留错误的乐观状态。

### 身份与 Profile 管理

**目标：** 增加 Profile 编辑、创建身份、明确的备份/恢复和安全的多身份切换。

**验收标准：**

- 私钥不进入日志、URL、统计系统或未加密浏览器存储。
- 每次写操作前，用户能明确识别当前 Relay 和公钥身份。
- 备份与恢复包含不可逆丢失提示，并有端到端恢复测试。

## P1：Agents 与 Projects

### 远程 Agent 运维

**目标：** 通过有认证的控制面提供远程 Agent 配置、日志、Session、Memory、Persona、
Team、响应策略和生命周期状态。

**验收标准：**

- 每个控制请求都经过 NIP-98 签名、allowlist、重放保护和审计。
- 离线、启动中、在线、降级和失败状态清晰区分，并提供恢复操作。
- 日志和配置会脱敏 Provider 凭据、其他频道 Prompt 和 `nsec`。
- 浏览器代码永远不尝试启动用户本机进程。

### 完整 Projects 工作流

**目标：** 补齐多级目录浏览，并增加项目发布/导入、Issue、PR、Review、Merge 和受权限
控制的写操作。

**验收标准：**

- 向 Relay 发布仓库公告时保留已有 Git remote。
- 展示写操作前验证 NIP-34 事件归属和 Git 权限。
- Branch、Issue 和 PR 页面由真实 Relay 事件驱动更新。
- HTML 仓库预览继续保持用户主动触发和 opaque-origin sandbox。

## P2：Inbox、Forum 与 Workflow

### Inbox 效率能力

**目标：** 增加会话分组、精确消息定位、提醒、稍后提醒、草稿和项目动态。

**验收标准：**

- 打开 Inbox 条目能够定位到准确消息或线程回复。
- Reminder 明确说明数据归属和同步规则。
- 大量 Inbox 数据分页后仍保持稳定选择和正确未读状态。

### 完整 Forum 体验

**目标：** 实现 Topic 创建、元数据、排序、审核和 Forum 特有的未读逻辑。

**验收标准：**

- 只有 Relay 启用功能或已经存在 Forum 频道时才展示入口。
- Topic 与回复可以和 Buzz Desktop 互操作。
- 审核操作按角色控制，并由真实 Relay 集成测试覆盖。

### Workflow 编辑和运行

**目标：** 增加 Workflow 列表、编辑、Trigger、Run/Step 详情、Schedule 和 Webhook；
现有 Inbox 审批继续作为第一类操作入口。

**验收标准：**

- 发布前验证 Workflow 定义。
- Run 状态实时更新，并可追踪到触发事件。
- Webhook 密钥不会返回或持久化到浏览器。

## P3：平台能力

后续研究方向包括多社区切换、Web Push、Pulse、基于 WebRTC 的 Huddle 和受控远程
Terminal。这些能力需要浏览器原生安全设计，不能直接复制 Tauri API。

进入实现前，每项能力都必须有威胁模型、协议兼容性决策，以及 Relay 未声明对应能力时的
降级方案。

## 持续工程工作

以下要求贯穿所有阶段：

- 在统一的认证传输契约下整合长连接和一次性 Nostr Client，同时保留两种不同生命周期。
- WebSocket、HTTP、媒体、邀请和 Git 路径始终以运行时 Relay 配置为准。
- UI 文件达到仓库 1,000 行硬限制前主动拆分，优先使用 feature 内部组件和 hook。
- 每条新增界面文案同时提供英文和简体中文，并由 Playwright 覆盖两种浏览器 locale。
- 增加可访问性、键盘导航和响应式视觉回归检查。
- 正式发布版本前，在 CI 增加依赖、密钥和容器扫描。

## 明确不做

- 在 Web 镜像中打包或运行 Buzz Relay。
- 把模型 Provider 凭据或 Nostr 私钥发送给 Agent Control。
- 把 Buzz Desktop 的设备本地实验开关当成跨客户端 Relay 配置。
- 展示没有真实后端操作或没有权限校验的占位控件。
